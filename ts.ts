import React from 'react';
import PropTypes from 'prop-types'

const SHOULD_BE_UNUSED = 1;

const INPUT_PASS_PROPS = {
    autoFocus: true,
    disabled: true,
    warning: true,
    error: true,
    size: true,
    onKeyDown: true,
};

console.log(PropTypes.number);
console.log(React.Component);
console.log(INPUT_PASS_PROPS);
